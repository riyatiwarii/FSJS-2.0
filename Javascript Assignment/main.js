import {countries}  from "./countries";
import {popularWebTech} from "./web_techs.js"

console.log(countries);
console.log(popularWebTech);

// Followed these steps

// Installed package
// npm install esm

// Command line to run the file in terminal.
// node -r esm main.js